#include<stdio.h>
#include <iostream>     // Input and output (cin, cout)
#include <iomanip>      // IO manipulators (setprecision, setw)
#include <string>       // std::string class
#include <cmath>        // Math functions (sqrt, pow, sin, cos, etc.)
#include <cstdlib>      // General utilities (rand, malloc, etc.)
#include <ctime>        // Time functions (time, clock)
#include <cstdio>       // C-style I/O (printf, scanf)
#include <vector>       // Dynamic array
#include <array>        // Fixed-size array
#include <deque>        // Double-ended queue
#include <list>         // Doubly linked list
#include <stack>        // Stack (LIFO)
#include <queue>        // Queue (FIFO), priority_queue
#include <set>          // Ordered set
#include <unordered_set>// Unordered set (hash set)
#include <map>          // Key-value pairs, ordered
#include <unordered_map>// Unordered key-value pairs (hash map)
#include <algorithm>    // Sorting, searching, etc.
#include <iterator>     // Iterators
#include <numeric>      // Numeric operations (accumulate, iota, etc.)
#include <utility>      // std::pair, std::swap
#include <functional>   // std::function, function objects
#include <bitset>       // Bit manipulation
#include <limits>       // Numeric limits
#include <typeinfo>     // Type information (typeid)
#include <cassert>      // Assertions (assert)
#include <stdexcept>    // Exceptions (runtime_error, etc.)
#include <fstream>      // File input/output
#include <sstream>      // String stream (stringstream, istringstream)
#include <cctype>       // Character classification (isalpha, etc.)
#include <cstring>      // C-style string manipulation



// #include<bits/stdc++.h>
// using namespace std;


#include <iostream>
#include <stack>
#include <algorithm>
using namespace std;


int main(

    

    return 0;
)