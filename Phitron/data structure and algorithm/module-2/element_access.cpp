// vector
    // V.begin();
    // V.end();

#include<bits/stdc++.h>
using namespace std;
int main(){
    int N= 9;
    vector<int>V(10);
    V.back();// last element.
    V[V.size()-1]; // last element
    V[0];  // fast element
    V.front(); // fast element
    V[N];// for Nth index element.

return 0;
}
