// the algorithm dosen't support negitive weighted cycle or negitive weighted undirected grap.
 #include<bits/stdc++.h>
 using namespace std;
 
 int main(){
 
     vector<int>v[5];
     v->resize(10);
        v[5].push_back(242);
     cout<<v[5].size();
 
 return 0;
 }