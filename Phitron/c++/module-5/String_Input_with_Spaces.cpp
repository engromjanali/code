#include<bits/stdc++.h>
using namespace std;
int main(){

    int x;
    cin>>x;
    cin.ignore();// to ignore "inter" we can use it, it's ignore a single character. it's not workable for every machine.
    //getchar();//2nd way.
    string s;
    getline(cin,s);

    cout<<x<<endl;
    cout<<s<<endl;
return 0;
}